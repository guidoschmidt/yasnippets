# -*- mode: snippet -*-
# name: build-add-exe
# key: build-add-exe
# --
const exe = b.addExecutable(.{
    .name = "$1",
    .root_module = b.createModule(.{
        .root_source_file = b.path("$2"),
        .optimize = optimize,
        .target = target,
    }),
});
b.installArtifact(exe);