# -*- mode: snippet -*-
# name: build-add-exe
# key: build-add-exe
# --
const target = b.standardTargetOptions(.{});
const mode = b.standardReleaseOptions();
const source = "./main.zig";
const exe = b.addExecutable("NAME", source);
exe.setBuildMode(mode);
exe.setTarget(target);
exe.install();
